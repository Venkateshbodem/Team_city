

publish-to-beta:
  image: nginx
  tags:
    - infy-aws-docker-linux
  stage: release
  only:
    - branches
    - /^release-.*$/
  except:
    - master
  when: manual
  script:
    - ./gradlew publish -PpublishBetaArtifactsFromTaskBranch=true --info
