

publish-to-beta:
  image: nginx
  stage: release
  only:
    - branches
    - /^release-.*$/
  except:
    - master
  when: manual
  script:
    - ./gradlew publish -PpublishBetaArtifactsFromTaskBranch=true --info
